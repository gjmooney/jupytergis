{
  "type": "object",
  "description": "WindParticleLayer",
  "title": "IWindParticleLayer",
  "required": ["source"],
  "additionalProperties": false,
  "properties": {
    "source": {
      "type": "string",
      "description": "The id of the GeoTiffSource (2-band U/V)"
    },
    "opacity": {
      "type": "number",
      "description": "The opacity of the layer",
      "default": 1,
      "multipleOf": 0.1,
      "minimum": 0,
      "maximum": 1
    }
  }
}
